# @ptl/platform-react

React bindings for `@ptl/platform-core`.

This package is intentionally generic. It can render platform commands, menus,
shortcuts, and registered React components, but it does not know about subtitles,
workbench layout, or a specific design system.
